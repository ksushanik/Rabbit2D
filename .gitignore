# Godot 4+ specific ignores
.godot/

# Mono-specific ignores
.mono/
data_*/
mono_crash.*.json

# Asset library ignores
.config/godot/

# Files imported by Godot (better handled by export templates)
# *.import # Let's keep .import files under version control for better collaboration

# Build results
export_presets.cfg
export.cfg # Included from remote

# Optional user data
export_credentials.cfg

# Imported translations (automatically generated from CSV files)
*.translation

# VS Code specific ignores (if used)
.vscode/

# Rider specific ignores (if used)
.idea/
*.sln
*.csproj

# Heap Snapshot files (memory debugging)
*.heapsnapshot

# History for UI/tool scripts in the editor
.history/
